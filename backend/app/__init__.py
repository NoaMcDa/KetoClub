"""KetoClub backend: menu proxy, hosted classification and community data.

The service is an accelerator, never a dependency. With no backend URL
configured the Flutter app behaves exactly as it does without this service
running at all. See `backend_plan.md` in the repository root.
"""

__version__ = "0.1.0"
